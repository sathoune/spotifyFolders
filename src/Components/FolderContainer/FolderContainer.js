import React, {Component} from 'react';
import "./FolderContainer.css"

class FolderContainer extends Component {
	constructor(props){
		super(props);
	}
	
	render(){
		return(<div className="folder-container">
			This if folder Container
		</div>)
	}
}

export default FolderContainer;